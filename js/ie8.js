$(window).load(function () {
	/*
	// ie users don't get the pretty animation because their browser is buggy as heck
	$('.menu').mouseenter(function () {
		$(this).children('.navMenu').stop().slideDown(500);
	}).mouseleave(function () {
		$(this).children('.navMenu').stop().slideUp(500);
	});*/
});
